// This will run once the page has finished loading

// The function is not executed by itself because someone or something has to execute it
function functionName() {
    element.event = FunctionName;
    element.event = FunctionName;
}
// Once all the content of the htm is loaded the function will be called and executed
window.onload = functionName; // global code